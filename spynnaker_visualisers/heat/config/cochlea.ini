// Simulation Information:
simparams = "COCHLEA";


COCHLEA = 
{
  SIMULATION=13;      // 13=COCHLEA
                      // parameters below specify the parameters for this visualisation type
  TITLE = "Real-Time Silicon Cochlea Output";   // What to plot across the top of the plot
  XDIMENSIONS=8;      // In a 2D plot, the total number of elements in the X direction
  EACHCHIPX=1;        //   how many of these each chip in the x dimension contributes
  YDIMENSIONS=64;     // In a 2D plot, the total number of elements in the X direction
  EACHCHIPY=1;        //   how many of these each chip in the y dimension contributes
  N_PER_PROC=256; //64*4
  WINHEIGHT = 600;	  // overall height
  WINWIDTH = 650;     // overall width (excluding key)
  HIWATER = 8.0;	  // for fixed scaling: the maximum, for dynamic scaling: the Starting HiWater value.
  LOWATER = 0.0;      // for fixed scaling: the minimum, for dynamic scaling: the starting LoWater value. 
  MAXFRAMERATE=10; 	  // how fast you want to refresh the graphics (max rate - frames per second)
  SDPPORT=54321;      // UDP port number used for SDP
  FIXEDPOINT=0;       // number of bits in word of data that are to the right of the decimal place
  DECAYPROPORTION=0.6;// how quickly does the plot decay (used for visualisers predominantly)
  BLACKBACKGROUND=1;  //Draw black background
  DYNAMICSCALE =1;    //Stop it rescaling
  DISPLAYMINIPLOT=0;  // set to zero if you do not wish to display the small tiled version in the bottom left of the screen 
  INITZERO=1;         // 0=if data is to start undefined, or set to 1 if datas default value should be zeroed. 
  DISPLAYXLABELS=1;   // set to zero if you don't want to display the labels and text on the x axis 
  DISPLAYYLABELS=1;   // set to zero if you don't want to display the labels and text on the y axis
  PERCENTAGESCALE=1;  // make this a 1 if you would like to show the output based on a 0-100% scale rather than the actual values (note: actual values still in place which # will display)
};


